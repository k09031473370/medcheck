# 健診ナビ 結果取込ツール 引き継ぎ資料

芝浦健診クリニック / KENSHIN navi 11
最終更新 2026/07/31

---

## 1. 何をするものか

健診の結果が入ったExcelを読んで、**健診ナビのDBに直接書き込む**ツール。手入力の置き換え。

健診ナビ本体には手を入れない。外部のPowerShellスクリプト群として `C:\Users\User\Documents\excel_tools` に置いてある。

### なぜDBに直接書くのか

健診ナビの「検体検査結果取込」を使おうとしたが、`列 'I' が無効です` で落ちた。
`UPDATE T_KENSA SET KEKKA=... WHERE PK_SEQ=... AND KOMOKU_CD=...` が実機で確実に動くことを確認したので、そちらを採用した。

---

## 2. 環境

| 項目 | 値 |
|---|---|
| DB | SQL Server `KNSV\SQLEXPRESS` / DB名 `K166_SIBAURAUSER` |
| 接続情報 | `\\KNSV\KenshinNavi\SQLSV\SQLServerConnect.txt` を読む |
| ツール置き場 | `C:\Users\User\Documents\excel_tools`（OneDrive配下） |
| 起動 | `start.bat` をダブルクリック → WinForms GUI |
| PowerShell | 5.1（.ps1 は UTF-8 BOM、.bat は cp932 で保存すること） |

**注意**: 事務所PCは日付表示が `M/d/yyyy` 形式。OneDrive同期配下。

---

## 3. ファイル構成

```
excel_tools\
  start.bat                 GUI起動
  form_import_gui.ps1       WinForms GUI（フロントエンド）
  form_import.ps1           取込エンジン（本体・約1200行）
  xlsx_read.ps1             xlsxをExcelなしで読む共通部品
  yoyaku_export.ps1         予約取込用Excelの生成
  ecg_import.ps1            心電図CSVの取込
  db_tool.ps1               DB調査用（SELECT専用 / -FindText / -Delete）
  restore_backup.ps1        バックアップCSVから T_KENSA を元に戻す
  check.bat 〜 check6.bat    調査用（ダブルクリックで結果をメモ帳表示）
  受付番号リスト_ひな形.xlsx  氏名+受付番号だけの一覧のひな形
  予約取込フォーマット.xlsx   ★健診ナビからもらった原本を置く（同梱していない）
  取込マニュアル.docx/.md     事務員向け手順書
  form\
    mapping_rian.csv         リアンパターン（106列）
    mapping_shibaura_ai.csv  芝浦巡回AIデータ（44列）
    mapping_srl.csv          SRL血液（45列・見出し有無どちらも可）
    mapping_ukeno.csv        受付番号リスト（3列）
    code_map.csv             自覚症状のコード変換
    vision_map.csv           視力の種別（1=裸眼 2=矯正 3=矯正）
    yoyaku_settings.csv      予約取込の共通値（住所・時間など）
    yoyaku_course.csv        コース変換
    yoyaku_dantai.csv        団体名→事業所コード
  backup\                    書込前の自動バックアップ（CSV）
```

---

## 4. 作業フロー（リアンの場合）

```
① 受診者取込   ツール「予約取込ファイルを作成」→ 健診ナビ「予約データ取込」で登録
①-b 受付番号   ツール「受付番号を設定」（氏名照合でUKE_NO_KENSAを書く）
② 結果取込     ツール「3.プレビュー」→「4.書込実行」
③ 血液取込     ツール（SRLのExcel）+「名簿でしぼり込む」
④ 自動判定     健診ナビ（BMI・標準体重・判定はここで計算される）
```

**①-b が必要な理由**: 予約取込フォーマットに受付番号の列が無い。予約登録では番号が入らないので、後から氏名照合で入れる。

**④は必ず最後**。血液を入れる前に判定を回すと血液抜きの判定になる。

芝浦巡回AIデータは氏名も受診日も無いので②のみ。受付番号が未設定なら `受付番号リスト_ひな形.xlsx` に氏名+番号を書いて「受付番号を設定」を使う。

---

## 5. データレイアウト

### 5-1. リアンパターン（106列・見出しあり）

主な列: 2=受付番号 / 3=受診日 / 7=氏名 / 8=フリガナ / 17-20=身長体重体脂肪腹囲 / 23-26=視力 / 27-30=血圧 / 32-34=尿 / 38-41=聴力 / 42=胸部X線フィルムNo / 46=空腹 / 50=診察判定 / 51-54=他覚所見 / 55-56=生理妊娠 / 57-59=自覚症状 / 60-71=既往歴家族歴（**取込不可**） / 72-93=問診Q1-22 / 95-100=胸部X線 / 101-105=心電図 / 106=コース

- 問診13/16/22 は `084013/084016/084022` ではなく **`H30-0061` / `H30-0062` / `R06-0004`**
- 既往歴(60-65)・家族歴(66-71)は T_KENSA に枠が無く取り込めない（健診ナビでは別テーブル管理）
- 自覚症状は `code_map.csv` でフォームのコード(010〜260)を健診ナビの結果CDへ変換。**9種が未割当**（020鼻づまり/050視力低下/110動悸息切れ/130むくみ/150胃腹痛/160下痢便秘/190かゆみ/200発疹/260物忘れ）

### 5-2. 芝浦巡回AIデータ（46列・見出しあり）— 2026/08/02 実データで確定

`受付NO / Q1-22 / 食事時間 / 身長 / 体重 / 腹囲 / 血圧×4 / 視力(種別+値)×2 / 聴力×4 / 聴力備考 / 尿×3 / 診察 / 胸部レントゲン / 心電図`

**氏名も受診日も無い。**

実データ `0615_0617.xlsx`（48人）で分かったこと:
- **46列だった**。旧対応表は44列想定で「視力備考」列があると思っていたが実際には無く、
  36列目から1つずれていた（聴力右1000Hz が視力備考扱いになっていた）。修正済み
- **受付NO は 1〜48 の連番**。以前の想定「40〜61」は誤り
- **診察(44)・胸部レントゲン(45)・心電図(46) の列がある**。この48人は全員 `1`。
  SHOKENCD として 017101A(SHIN) / 077010A(ZK020) / 067112A(ZK011) に割当てた。
  **「1」がそれぞれの所見リストで何と表示されるかはプレビューで未確認**
- **視力種別は 1=裸眼 / 2・3=矯正 で確定 (2026/08/08)**。値の分布からは判別できなかったが、
  旧自動入力ツール `kensin_navi_input_v2.py`（pyautoguiで画面に打ち込む方式・実運用実績あり）に
  「種別が1=裸眼ならここに入力、2or3=矯正なら矯正欄へ」と明記されていた。vision_map.csv と一致
- ファイル名が 0615_0617 で2日分が1ファイルに見えたが、**実運用は1日1ファイルにする**と
  業務側が決めた(2026/08/02)。もし2日分が混ざったファイルが来たら、
  受付番号欄で範囲を分けて2回取り込む
- **受診日はA列に入れる運用を推奨 (2026/08/02)**。芝浦は氏名が無く、GUIの受診日を
  打ち間違えると氏名照合が効かないまま別の日の同番号の人に書き込まれ得るため。
  `mapping_shibaura_ai2.csv`(47列・A1=受診日)を追加し、`Detect-MappingPath` は
  HEADERキーワードが多い対応表を優先するようにした(受診日,受付NO,Q1 の3語 > 受付NO,Q1 の2語)。
  日付を入れ忘れた素の46列も従来どおりGUIの受診日欄で取り込める。
  日付は**全員の行に**入れること(空の行はエラーで止まる。安全側)

### 5-3. SRL血液（45列・見出しあり・パスワード `srl`）

| 列 | 内容 | | 列 | 内容 |
|---|---|---|---|---|
| 1 | 受付日 (YYMMDD) → 受診日 | | 17 | ALP |
| 2 | 報告日 | | 18 | γ-GTP |
| 3 | 受付Ｎｏ（SRL側の検体番号・使わない） | | 19 | 血糖 |
| 4 | 氏名カナ | | 20 | HbA1c |
| 5 | **カルテ№ → 当院の受付番号** | | 21 | 白血球数 |
| 6 | 総蛋白TP | | 22 | 赤血球数 |
| 7 | アルブミン | | 23 | 血色素量 |
| 8 | 尿素窒素BUN | | 24 | ヘマトクリット |
| 9 | クレアチニン | | 25 | 血小板数 |
| 10 | 尿酸 | | 26-28 | Na / K / Cl |
| 11 | 総コレステロール | | 29 | eGFR |
| 12 | LDL | | 30-32 | MCV / MCH / MCHC |
| 13 | HDL | | 33 | MCIスクリーニング（枠なし） |
| 14 | 中性脂肪 | | 34-35 | 便潜血1回目 / 2回目 |
| 15 | AST(GOT) | | 36-45 | 栄養・リスクレベル等（枠なし） |
| 16 | ALT(GPT) | | | |

**確定方法**: 頃安さん(4002)の7/2の値を健診ナビの「結果(前回)」と突き合わせて11項目が完全一致。HDLと中性脂肪の並びは4人分をFriedewald式（LDL≒TC−HDL−TG/5）で検算して確認（磯野166 vs 実測169、長島75 vs 78。逆にすると大きくずれる）。

**1ファイルにリアン以外の人も混ざる**（7/2は39人中13人が名簿外）。「名簿でしぼり込む」でリアンのExcelを指定して除外する。

---

## 6. DBの知見

### テーブル

| テーブル | 用途 |
|---|---|
| `T_KENSA` | **検査結果の本体。ここに書く。** PK_SEQ + KOMOKU_CD で1行 |
| `T_KENSA_MULTI` | 結果入力画面を開いている間の作業用コピー。**書いてはいけない** |
| `T_MULTI` | 排他ロック。書込前にここを見て、開いていたら中止する |
| `T_KENSIN` | 受診情報。`UKE_NO_KENSA`(受付番号) `D_KENSIN`(受診日) `KOJIN_ID` `COURSE_CD` `DANTAI_CD1` `F_TORIKESI` |
| `T_KANJA_G` | 受付済みの人。`KEN_YMD` + `KEN_NO` → `PK_SEQ` |
| `T_KOJIN1` | 個人マスタ。`KANJI_SIMEI` `KANA_SIMEI` |
| `T_KOMOKU` | 項目マスタ。`KOMOKU_CD` `MEISYO1` `SYOKEN_CD` |
| `T_SYOKEN2` | 選択肢/所見マスタ。`SYOKEN_CD` `KEKKA_CD` `SYOKEN` `HANTEI_KIGO` |
| `T_COURSE1` | **コースマスタ。`COURSE_CD` + `DANTAI_CD1` の組で1行。** `MEISYO` |
| `M_HENKAN_IRAI` | 外注コード変換（江東微研）。`CENTER_CD` `OUT_CD` `KOMOKU_CD` |

### 人の特定

```
1) T_KANJA_G  KEN_YMD + KEN_NO        → PK_SEQ   (受付済み)
2) T_KENSIN   D_KENSIN + UKE_NO_KENSA → PK_SEQ   (未受付。F_TORIKESI=0)
```
未受付でもT_KENSAの枠は予約時点で作られているので書き込める。

### 判明しているコード

| 項目 | KOMOKU_CD |
|---|---|
| 身長/体重/体脂肪/腹囲 | 001211 / 001212 / 001213 / 001215 |
| 血圧 初回上下・2回目上下 | 001253 / 001254 / 001255 / 001256 |
| 視力 裸眼右左・矯正右左 | 067012 / 067013 / 067018 / 067019 |
| 聴力 右左1000・右左4000 | 067132 / 067134 / 067133 / 067135 |
| 尿 糖・蛋白・潜血 | 069207 / 069206 / 069211 |
| 空腹 | 001282 |
| 他覚所見A/B/C | 017101A / B / C（SYOKEN_CD=SHIN） |
| 自覚症状A/B/C | 017107A / B / C |
| 生理中 / 妊娠中 | 083002 / 083003 |
| 胸部X線 フィルムNo / 所見A / 所見B | 077010Z / 077010A / 077010B（ZK020=部位 ZK021=所見） |
| 心電図 所見A/B/C | 067112A / B / C（ZK011） |
| 問診01-22 | 084001〜084022（13/16/22は H30-0061 / H30-0062 / R06-0004） |
| クレアチニン / 尿酸 / eGFR | 068674 / 068675 / 068676 |
| 白血球/赤血球/血色素/ヘマトクリット | 069003 / 069004 / 069005 / 069006 |
| MCV / MCH / MCHC / 血小板 | 069007 / 069008 / 069009 / 069010 |

| 総コレ / LDL / HDL / 中性脂肪 | 068705 / 068709 / 068708 / **R06-0002** |
| AST / ALT / ALP / γ-GTP | 068636 / 068637 / 068645 / 068652 |
| 血糖 / HbA1c | **068682** / 068689 |
| 便潜血 1回目 / 2回目 | 069245 / 069246（値は「陰性」→ (-) に変換） |

**注意**: 中性脂肪は `068702 空腹時中性脂肪` ではなく `R06-0002 中性脂肪(未判別)`、
血糖は `H30-0011 空腹時血糖` ではなく `068682 血糖(未判別)`。7/2の実データに合わせた。

**枠が無い項目**（このコースでは測っていない）: 総蛋白TP・アルブミン・BUN・Na・K・Cl

### コースと事業所（リアン）

| Excelのコース | 事業所 | 事業所コード | COURSE_CD | コース名 |
|---|---|---|---|---|
| 健保外 | 木下福祉アカデミー | 0000000233 | YA | 東京実業A |
| B | 木下の介護 | 0000000230 | YB | 東京実業B |

---

## 7. 実装上の落とし穴（すべて実機で踏んだもの）

### Excel COM は使わない
OneDrive上・外部ドライブ上のxlsxを `Workbooks.Open` すると、保護ビュー等のダイアログが**非表示のまま出て永久に固まる**。
→ xlsxはZIP+XMLなので `xlsx_read.ps1` で直接読む。Excelを使うのは「予約取込Excelの書き出し」と「パスワード解除」だけ。

### xlsx読み取りの注意
- `XmlDocument` も `XmlNamespaceManager` も IEnumerable なので、関数から返すときに `,` を付けないとPowerShellが展開してしまう
- 共有文字列は `m:t | m:r/m:t` だけを連結する。`m:rPh/m:t` は**ふりがな**なので混ぜると「健保外」が「健保外ケンポガイ」になる
- シートの `dimension` は104万行などデタラメなことがある。実際に `<row>` がある分だけ読む

### Excelへの書き込み
セルを1つずつ `Cells.Item(r,c).Value2 = 文字列` で埋めると `指定されたキャストは有効ではありません`（InvalidCastException）。
→ `object[,]` を作って `Range.Value2` に一括代入する。速度も桁違い。

### 予約取込フォーマット
- テンプレートによって**見出し行の位置（1行目/2行目）と列構成が違う**（年齢・社員番号・所属名の有無）。列番号を決め打ちにせず、見出しの文字から列を探すこと
- **事業所コードが必須**。空だと事業所が特定できず、連鎖でコースも解決できずコース欄が「形式不正」の赤になる
- 生年月日・予約日は文字列（`2026/07/05`）でよい

### 日付
- `M/d/yyyy` 形式のPCでCSV経由にすると月日が入れ替わる危険。xlsx直読みなら数値のまま読めるので安全
- `260705` `20260705` `2026/07/05` すべて受け付ける
- ファイルに日付列があっても、GUIの受診日欄に入力があればそちらを優先し、確認ダイアログを出す

### IfEmpty / ReqCol / ReqVal (「検査したが所見が空」→ 既定値)
- `IfEmpty` = 入れる結果CD、`ReqCol` = 実施を示す列、`ReqVal` = その列がこの値のときだけ実施とみなす
- `ReqVal` は `|` 区切りで複数可（CSVなのでカンマは使えない）
- `ReqVal` が空だと「ReqCol が空でなければ実施」になる。**書ける列は必ず書くこと**
  未実施を示すコードが入る列だと、未実施を「異常なし」にしてしまう
- 既定値で入れた行はプレビューに `OK(既定値)` と出て、件数も集計に出る

### 安全策（実装済み）
- 書込前に `T_MULTI` を見て、結果入力画面が開いていたら中止
- **`sp_getapplock`**（トランザクション単位）。`T_MULTI` は健診ナビ画面のロックしか見ないので、
  ツールの二重起動・別PCからの同時実行はこれで止める。ロック取得後にもう一度 `T_MULTI` を確認する
- 書込前のデータを `backup\` にCSV保存 → `restore_backup.ps1` で戻せる
- トランザクション。影響行数が1でなければロールバック
- **氏名照合**。Excelの氏名と健診ナビの氏名（カナ優先）が違う人はスキップ。受付番号がずれていると別人を上書きしてしまうため
  正規化は .NET の `FormKC` + ひらがな→カタカナ。`Microsoft.VisualBasic` には依存しない（環境により黙って無効化されるため）
- **プレビューと書込の同一性**。ファイル・更新日時・サイズ・受付番号・受診日・名簿・レイアウト・
  各チェックから指紋を作り、プレビュー時と違えば警告する
- **非常口**（`-Force` / `-IgnoreName`）は使用時に内容を列挙して再確認し、書込のたびにチェックを自動で外す
- **受付済み（`T_KANJA_G` に行がある）の人は受付番号を変更しない**。健診ナビ側が別に番号を持つため
- 名簿でしぼり込み（`-Roster`）。血液など名簿外が混ざるファイル用。照合はPK_SEQで行う

### あえて採用しなかったこと
- 「本番DB（`K166_SIBAURAUSER`）では `-IgnoreName` を禁止」
  → このクリニックにはテスト用DBが無く、**本番DBでテストしている**（テスト用受診者を本番に作る）。
    禁止するとテストができなくなるため、代わりに「毎回リセット＋明示確認」にした

### 配布ZIP
UTF-8フラグ付きZIPだとWindowsのエクスプローラーで日本語ファイル名が化ける。
→ `make_zip.py` がファイル名をcp932で書き、UTF-8フラグを立てない。

---

## 8. 現在の状況

### 動いているもの
- **リアンパターンの一連すべて**（①受診者取込 → ①-b受付番号 → ②結果取込 → ③血液取込 → ④自動判定）
  2026/07/05のテストデータ26人で通し確認済み
- SRL血液の取込（項目コードは7/2の実データと値を突き合わせて確定。便潜血の選択肢変換も動作確認済み）
- 芝浦巡回AIデータの取込
- 心電図CSVの取込
- 受付番号の一括設定（氏名照合）
- 予約取込Excelの生成

### ★心電図の自動補完は復活させた (2026/08/02・決着)

`check7.bat` を本番DBで実行した結果、**2026/04/01〜07/24 の47日・約340人で、
心電図の枠がある人は全員に所見が入っていた**（空欄は 07/31 の3人＝受付番号 4/40/41 のみで、
これは未取込分。40/41は芝浦巡回の番号）。

事前に決めた基準（枠がある人数＝所見が入っている人数なら全員撮っている）を満たしたので、
`mapping_rian.csv` 101列を **`IfEmpty=1` / ReqCol・ReqVal なし** に戻した。
枠があること自体が実施の証拠、という判断。

撮らない人が出るようになったら `ReqCol=105` `ReqVal=1` を足せば、
「判定欄に1を書いた人だけ補完」の安全側に戻せる（下記の経緯を参照）。

<details><summary>そこに至るまでの経緯 (2026/07/31)</summary>

リアンの **44列 心電図検査№ は受付番号のコピー**（42列の胸部X線フィルム№も同じ）。
74人分の実データで確認済み。つまり「44列に値がある＝心電図を撮った」とは言い切れない。

リアンの106列を全部調べたが、**「心電図を撮ったか」を示す列は無い**。
105列の判定も、今のところ異常があった人にしか入らない（0712の48人＝所見も判定も空が41人、
残り7人がコード+判定 2/3/4）。

「枠はあるが撮らない人がいるかもしれない」と業務側から回答があったため、
44列を実施印にする案は破棄。代わりに業務側から
「異常のない人には異常なしの1を入れればいいか」と提案があったので、
**`mapping_rian.csv` 101列を `IfEmpty=1` / `ReqCol=105` / `ReqVal=1` にした**。

- 所見(101列)に値がある → そのまま入る（従来どおり）
- 所見が空 かつ 判定(105列)が `1` → 正常範囲(ZK011の結果CD 1)を入れる
- 所見が空 かつ 判定が空/2/3/4 → **何も入れない**

判定欄に1を入れる運用が始まっていなくても副作用は無い（該当者0人になるだけ）。
所見欄に直接 `1` と書いた場合も、SHOKENCD の通常経路で正常範囲になる。

心電計のCSV（`心電図取込.bat`）から入れる方法も従来どおり使える。
このCSVには実際に測定した人しか出てこないので、実施の有無で迷わない。

全員必ず撮ると確認できたら `ReqCol` / `ReqVal` を外せば全員に既定値が入る。
`check7.bat` で過去の実績（枠がある人数 vs 所見が入っている人数）を確認できる。

95列 撮影区(全員 `1`) と 50列 診察判定(`1`/`3`) は `ReqVal` で値を限定済み。
未実施を示すコードが来ても既定値は入らない。

</details>

### 自覚症状の対応表を確定した (2026/08/02)

`check3.bat` で MN010 の選択肢35種を取得。リアンのコード26種と突き合わせた結果:

**追加でつながったもの**
| リアン | 健診ナビ MN010 |
|---|---|
| 110 動悸・息切れ | 15 動悸や脈の乱れがある |
| 130 顔や足がむくむ | 13 手足がむくむ |
| 200 発疹 | 34 皮膚にできものができやすい |

**業務判断で「取り込まない」と決めたもの**（意味がずれる／複数に分かれて特定できない）
- 050 視力が低下した … 8「かすむ」に寄せる案は見送り
- 160 下痢や便秘 … 便秘25/下痢26 に分かれ、どちらか分からない
- 150 胃やお腹が痛む … 空腹時22/食後23/下腹部24 に分かれる
- 020 鼻づまり・鼻汁、190 かゆみ、260 物忘れ … MN010に該当なし
- 030 息苦しい、170 残尿感 … 実データに出現せず保留

0712の48人では、これで **27件の落ちが8件に減った**。

**同じ選択肢の二重書き込みを防ぐ処理を入れた** (`Build-Plan` の `$seenChoice`)。
040「眼が疲れる・かすむ」と050「視力が低下した」は健診ナビでは同じ8番になるため。
`017107A〜J` のように末尾が英字で並んでいる枠だけが対象で、
聴力(`067132`/`067133`/…)のような別項目は重複扱いしない。
2件目は `取込対象外(同じ内容が自覚症状1に入るため)` としてプレビューに出る。

### ★社員番号は T_KOJIN1.KOJIN_NO に決着 (2026/08/02)

**確定方法**: 健診ナビの個人マスタ画面で テストむつ子(KOJIN_ID=1004182) の
「社員番号」欄に 99999 を入れて登録 → `check11.bat`(db_tool の `-FindText`)で
DB全体を検索 → `T_KOJIN1.KOJIN_NO` に1件ヒット。

- 帳票の差込み文字 **`**社員番号`** は健診ナビが用意している
  （`\\KNSV\KenshinNavi\11_健康診断結果報告書\退避\701_新規 Microsoft Excel Worksheet.xlsx`
   が「ラベル｜差込み文字」41行の早見表になっている。`**ｶﾙﾃ` `**個人ID` `**血液型ABO` なども）
- **事業所名は対応不要**。`**事_名称` / `**事_ｺｰﾄﾞ` は主要な帳票に既に入っていて、
  `T_KENSIN.DANTAI_CD1` → `M_DANTAI.MEISYO1` から印字される。予約取込で入る。
- 部署も枠はある: `T_KOJIN1.BUSYO_CD` + `T_BUSYO`(事業所CD/部署CD/部署名)。**未対応**。
  リアンの部署コード `1-700376` は先方の体系なので、T_BUSYO への登録か対応表が要る。

**実装** (`Kind=KOJINNO`):
- `mapping_rian.csv` 1列目 → `UPDATE T_KOJIN1 SET KOJIN_NO WHERE KOJIN_ID`
- `Get-NaviName` が KOJIN_ID / KOJIN_NO も返すようにした（氏名照合と同じ1クエリ）
- **すでに別の番号が入っている人は上書きしない**（過去の結果票に波及するため）。
  `取込対象外(すでに別の番号 ○○ が入っています)` としてプレビューに出る
- `Backup-Kojin1` を追加。T_KENSA とは別に `T_KOJIN1_<KOJIN_ID>_<日時>.csv` を残す
- `restore_backup.ps1` は KOJIN_ID 列があって KOMOKU_CD 列が無いCSVを個人マスタ用と判定し、
  **KOJIN_NO だけ**戻す（氏名や住所まで巻き戻すと手直しが消えるため）
- `-List` は T_KENSA_*.csv と T_KOJIN1_*.csv の両方を出す

**残: 結果票への印字**。テンプレートに `**社員番号` を1セル足す作業は未実施。
帳票を触るので業務判断待ち。DB側には入るので、足せばすぐ出る。

<details><summary>調査の経緯</summary>

リアンのExcelには揃っている: **1列目=社員番号 / 4列目=団体コード / 5列目=団体名 /
12列目=部署コード / 13列目=部署名**。0712の例では 43710 / 1 / 木下の介護 / 1-700376 / ｸﾞﾙｰﾌﾟﾎｰﾑ町田。

`check5.bat` で候補の欄は洗い出した（`T_KOJIN1.KOJIN_NO/KARUTE_NO/TECHO_NO`、
`T_KOJIN2.SYUSSEKI_NO/DIVISION/GYOSYU`、`T_KENSIN.FREE_CODE/BUSYO_CD/S_UKE_NO`）が、
どれが空いているかは未確認。**`check8.bat` の結果待ち**。

方針: 健診ナビの帳票テンプレート(`101_個人結果票B4.xlsx`)は保守の都合で触らない。
**すでに結果票に印字されている欄のうち、空いているもの**に社員番号を入れるのが第一候補。

`check8` で空いていた欄: `T_KOJIN1.TECHO_NO` / `T_KOJIN2.SYUSSEKI_NO` / `DIVISION` /
`GYOSYU` / `CLASS` / `GAKUNEN` / `S_UKE_NO` / `T_KENSIN.FREE_CODE` / `S_UKE_NO` / `BIKO`。
使用中: `T_KOJIN1.KOJIN_NO`(81件) / `T_KENSIN.BUSYO_CD`(158件) / `OCR_CODE`(4293件)。
81件は「正しい欄が一部の事業所でだけ使われている」状態だった(新島村の巡回)。

</details>

### 会社宛の一括請求 (請求一覧.bat) — 2026/08/09 初実行

サンテック 2026/06/26 で本番実行。58名・エラーなし。

| | 金額 |
|---|---|
| 会社請求 合計 | 370,530円 (基本 338,850 + オプション 0 + 調整 31,680) |
| 健保への請求(参考) | **462,258円** |
| 協会けんぽの実績(メール) | **459,726円** |
| 差 | **+2,532円 (0.55%)** |

内訳: 協会一般 通常20 / 胃欠14 / 便1本3 / 便欠2、若年2、定期A17。
PSA 16名 × 1,980円 = 31,680円。

**差2,532円の原因は未特定。** 分かっていること:
- 便の分類違い(差額 584/287/297円)の組合せでは 2,532 を作れない
- 1人まるごと胃欠⇔通常(7,130円)でもない
- 子宮頸がん検診の協会補助 2,530円 と 2円違い（偶然か要確認）
- **胃部X線と便潜血が両方とも未実施の3名(1006堤/1054山口/1055瀧本)** の正しい単価が不明。
  料金表に組合せの行が無く、暫定で胃欠(7,005)にしている。ここが怪しい

次にやること: 事務が協会へ出した明細と、`請求一覧.bat` が出すCSVを人単位で突き合わせる。

**判定ロジック**
- 胃部X線未実施 … `077300A` に結果が無い（枠が無い場合も含む。料金表に胃欠の行があるコースのみ）
- 便潜血 … `069245`(1回目) / `069246`(2回目) の結果がある本数
- PSA … `067818` に結果あり。受付の料金入力(T_RYOUKIN)に入っている人は二重加算しない
- 減額になった人は全員【減額になった人】として名前つきで表示し、目視確認を促す

### 残作業
1. 自覚症状の未割当9種（`check3.bat` で選択肢一覧を取れば埋められる）
2. 結果票への事業所名・社員番号の印字（`check5.bat` 待ち。帳票テンプレート `101_個人結果票B4.xlsx` 側の対応も要検討）
3. 東振協提出データの突き合わせ確認（八坂さんの正規品サンプルあり）
4. 総蛋白・アルブミン・BUN・Na・K・Cl は今のコースに枠が無い。別コースで必要になったら
   `check6.bat` と同じ方法で項目コードを確定して `mapping_srl.csv` に追記する

### git
`claude/new-session-4ac62k` ブランチに60コミット超。**pushは403で通っていない**（GitHub App の権限）。実体はZIPで配布している。

---

## 9. 禁止事項

- 健診ナビ本体（プログラム・マスタ）を書き換えない
- 江東微研のコード変換マスタ、`\\KNSV\KenshinNavi\CSV\検査センター.csv` は変更しない
- `T_KENSA_MULTI` には書かない
- プレビューを見ずに書込実行しない
- 取込対象者の結果入力画面を健診ナビで開いたままにしない
- `form\` のCSVをExcelで編集しない（先頭の0が消える）
- テスト中は健診ナビの「受付」ボタンを押さない（受付番号が自動採番で上書きされる恐れ）
